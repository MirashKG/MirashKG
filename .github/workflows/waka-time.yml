GH_TOKEN: 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
WAKATIME_API_KEY: waka_2e6bd7fd-277b-45ef-b34e-c745d2ea5b8a
name: WakaTime

on:
  schedule:
    # Runs at 12am IST
    - cron: "30 18 * * *"
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
